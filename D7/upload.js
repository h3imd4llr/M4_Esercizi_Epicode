const url = "https://striveschool-api.herokuapp.com/api/product/"

function upload(){
    let response = fetch(URL,
        {
            method: "POST"
            body: JSON.stringify({})
        }
        )
}